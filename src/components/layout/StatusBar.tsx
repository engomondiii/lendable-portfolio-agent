'use client';

// StatusBar is now removed from the layout — info moved to TopBar.
// This file is kept as an empty export to avoid import errors.
// You can safely delete StatusBar.tsx and remove its import from RootLayout.

import React from 'react';

export const StatusBar: React.FC = () => null;